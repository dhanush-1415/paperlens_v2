/**
 * `/security`, in three sections.
 *
 * Lifecycle, commitments, posture: where your file goes, what we promise about it, and what we
 * can and cannot prove. The order runs from the concrete to the institutional, because the
 * person this page has to convince is far more often someone holding a medical bill than
 * someone running a vendor review — and the first group leaves after the first section with
 * their question answered.
 */

export { SecurityCommitments } from './commitments';
export { SecurityLifecycle } from './lifecycle';
export { SecurityPosture } from './posture';
export type { SecurityPostureProps } from './posture';
